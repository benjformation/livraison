{% extends 'base.html.twig' %}

{% block title %}Suivi{% endblock %}

{% block body %}
    <h1>Suivi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Instant</th>
                <td>{{ suivi.instant ? suivi.instant|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>StatutColis</th>
                <td>{{ suivi.statutColis }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('suivi_index') }}">back to list</a>

    <a href="{{ path('suivi_edit', {'id': suivi.id}) }}">edit</a>

    {{ include('suivi/_delete_form.html.twig') }}
{% endblock %}
