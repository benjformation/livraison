{% extends 'base.html.twig' %}

{% block title %}TypeDepot{% endblock %}

{% block body %}
    <h1>TypeDepot</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_depot.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type_depot.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_depot_index') }}">back to list</a>

    <a href="{{ path('type_depot_edit', {'id': type_depot.id}) }}">edit</a>

    {{ include('type_depot/_delete_form.html.twig') }}
{% endblock %}
