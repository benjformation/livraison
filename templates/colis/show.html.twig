{% extends 'base.html.twig' %}

{% block title %}Colis{% endblock %}

{% block body %}
    <h1>Colis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Numero</th>
                <td>{{ coli.numero }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('colis_index') }}">back to list</a>

    <a href="{{ path('colis_edit', {'numero': coli.numero}) }}">edit</a>

    {{ include('colis/_delete_form.html.twig') }}
{% endblock %}
