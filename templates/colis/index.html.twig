{% extends 'base.html.twig' %}

{% block title %}Colis index{% endblock %}

{% block body %}
    <h1>Colis index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Numero</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for coli in colis %}
            <tr>
                <td>{{ coli.numero }}</td>
                <td>
                    <a href="{{ path('colis_show', {'numero': coli.numero}) }}">show</a>
                    <a href="{{ path('colis_edit', {'numero': coli.numero}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('colis_new') }}">Create new</a>
{% endblock %}
