{% extends 'base.html.twig' %}

{% block title %}StatutColis{% endblock %}

{% block body %}
    <h1>StatutColis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statut_coli.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ statut_coli.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('statut_colis_index') }}">back to list</a>

    <a href="{{ path('statut_colis_edit', {'id': statut_coli.id}) }}">edit</a>

    {{ include('statut_colis/_delete_form.html.twig') }}
{% endblock %}
